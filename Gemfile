# Gemfile for testing the Resilient AI Client examples

source 'https://rubygems.org'

gem 'stoplight', '~> 4.0'

# Real AI providers (direct clients used in legacy examples)
gem 'ruby-openai', '~> 7.0'
gem 'gemini-ai', '~> 4.0'

# Unified LLM interface used by this app
gem 'ruby_llm'

# Optional: Redis for production circuit state persistence
# gem 'redis', '~> 5.0'

# For loading environment variables
gem 'dotenv', '~> 3.0'
